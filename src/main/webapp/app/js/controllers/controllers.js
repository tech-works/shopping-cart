/**
 * 
 */
define([
        'angular',
        'angular-animate'
        ], function ( angular, ngAnimate ) {
    'use strict';
    
    return angular.module('iscart.controllers', ['ngAnimate']);
});